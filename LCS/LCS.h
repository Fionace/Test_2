#include<iostream>
#include<string.h>
using namespace std;

class LCS
{
	public:
		void  init(int **searcharr,int mslen,int nslen);
		void create(char *m,char *n,int **searcharr);
		void find(char *m,char *n,int **searcharr,char *sub);
	private:




};
